/*
 * GET home page.
 */
import staticRoot = require('./staticRoot');
import navBar = require('./navBar');

var controllers = {
    staticRoot: staticRoot,
    navBar: navBar,
};

export = controllers;
