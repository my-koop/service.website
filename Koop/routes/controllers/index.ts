/*
 * GET home page.
 */
import homepage = require('./homepage');
import user = require('./user');
import navBar = require('./navBar');

var ctrls = {
    homepage: homepage,
    user: user,
    navBar: navBar
};
export = ctrls;
